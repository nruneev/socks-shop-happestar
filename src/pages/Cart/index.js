import './index.sass'
import React from 'react';
import { CartContext } from '../../utils/contexts';
import { CartBlock } from './CartBlock';


const Cart = () => {

    return(
        <div className='page cart'>
            
        </div>
        )
    };
    
    // <Link className='menu-item' to='/cart' onClick={closeMenu}><GiFactory/>Корзина</Link>
